# Django settings
DEBUG=False
SECRET_KEY=tu-clave-secreta-aqui
ALLOWED_HOSTS=tu-dominio-render.com,www.midominio.com

# Configuración de base de datos (si usas PostgreSQL en producción)
DATABASE_URL=postgres://usuario:contraseña@host:puerto/nombrebd

# Email (opcional, para formularios o error reporting)
EMAIL_HOST=smtp.tu-servidor.com
EMAIL_PORT=587
EMAIL_HOST_USER=tu-usuario-email
EMAIL_HOST_PASSWORD=tu-clave-email
EMAIL_USE_TLS=True

# Google Maps API o similares (si usas mapas personalizados)
GOOGLE_MAPS_API_KEY=tu-api-key-opcional
